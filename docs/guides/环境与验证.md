# 环境与验证

## 创建环境

仓库固定使用 Python 3.11 和 uv：

```powershell
uv python pin 3.11
uv sync --all-groups
```

uv 会在仓库内创建 `.venv`，并按照 `uv.lock` 同步依赖。不要向系统 Python 安装本项目依赖。

## 验证骨架

```powershell
uv run surrogate-loop --help
uv run python -m surrogate_loop --version
uv run ruff check .
uv run pytest
```

预期版本输出为：

```text
surrogate-loop 0.1.0
```

本阶段只验证包入口、目录合同和工具链，不运行 ODE 求解或模型训练。
